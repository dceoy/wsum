interface:
  display_name: "Web Update Monitor"
  short_description: "Monitor website updates with selectable persistence"
  default_prompt: "Use $web-update-monitor to run a safe website update check with either Google Drive or local persistence."
