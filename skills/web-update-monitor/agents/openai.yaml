interface:
  display_name: "Web Update Monitor"
  short_description: "Monitor website updates with local filesystem state"
  default_prompt: "Use $web-update-monitor to run a safe local website update check and notify Slack when a meaningful change is found."
